import { NepaliDateFormaterPipe } from './nepali-date-formater.pipe';

describe('NepaliDateFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new NepaliDateFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
