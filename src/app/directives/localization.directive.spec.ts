import { LocalizationDirective } from './localization.directive';

describe('LocalizationDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalizationDirective();
    expect(directive).toBeTruthy();
  });
});
